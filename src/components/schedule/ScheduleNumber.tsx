const ScheduleNumber = ({ number }: { number: number }) => {
  return <div className="mr-4 w-8 h-8 bg-blue-500 text-white rounded-full flex items-center justify-center">{number}</div>;
};

export default ScheduleNumber;
