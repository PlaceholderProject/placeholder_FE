import React from "react";

const ProvidersLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default ProvidersLayout;

// 상위와 하위의 레이아웃 계층이 연결되지 않아 중간 레이아웃을 만들어줌!
